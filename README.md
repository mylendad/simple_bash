# Simple Bash — утилиты `cat` и `grep`

Учебный проект по разработке двух классических утилит командной строки Unix — `cat` и `grep` — на языке C (стандарт C11, компилятор gcc).

Проект состоит из двух независимых программ:

- `s21_cat` — вывод, объединение и создание текстовых файлов;
- `s21_grep` — поиск строк, соответствующих регулярным выражениям.

## Сборка

Сборка программ настроена с помощью единого `Makefile` в корне репозитория.

| Цель            | Описание                                        |
| --------------- | ----------------------------------------------- |
| `make all`      | сборка `s21_cat` и `s21_grep`                  |
| `make s21_cat`  | сборка только утилиты `cat`                    |
| `make s21_grep` | сборка только утилиты `grep`                   |
| `make tests`    | запуск интеграционных тестов                   |
| `make clean`    | удаление исполняемых файлов                    |
| `make rebuild`  | полная пересборка (`clean` + `all`)             |
| `make leaks_linux`   | проверка на утечки памяти (valgrind, Linux) |
| `make leaks_mac_os`  | проверка на утечки памяти (leaks, macOS)   |

Исполняемые файлы создаются в директориях `src/cat/` и `src/grep/`.

## Использование cat

`cat` — одна из наиболее часто используемых команд в Unix-подобных операционных системах. Команда имеет три взаимосвязанные функции в отношении текстовых файлов: отображение, объединение их копий и создание новых.

```
cat [OPTION] [FILE]...
```

### Опции cat

| №   | Опции                                                              | Описание                                                       |
| --- | ------------------------------------------------------------------ | -------------------------------------------------------------- |
| 1   | `-b` (GNU: `--number-nonblank`)                                    | нумерует только непустые строки                                |
| 2   | `-e` (предполагает `-v`), `-E` (GNU: без применения `-v`)          | отображает символы конца строки как `$`; для `\r\n` отображает `^M$` |
| 3   | `-n` (GNU: `--number`)                                             | нумерует все выходные строки                                   |
| 4   | `-s` (GNU: `--squeeze-blank`)                                      | сжимает несколько смежных пустых строк                         |
| 5   | `-t` (предполагает `-v`), `-T` (GNU: то же самое, но без `-v`)     | также отображает табы как `^I`                                 |

## Использование grep

### История

Томпсон написал первую версию на PDP-11 языке ассемблера, чтобы помочь Ли Э. МакМахону проанализировать текст «Записок Федералиста» и определить авторство отдельных статей. Текстовый редактор `ed` (также созданный Томпсоном) имел поддержку регулярных выражений, но его нельзя было использовать для такого большого объема текста, поэтому Томпсон извлек этот код в отдельный инструмент. Он выбрал это название, потому что в `ed` команда `g / re / p` печатала все строки, соответствующие заданному шаблону. `grep` впервые был включен в Версию 4 Unix. Заявив, что он «обычно упоминается как прототип программного средства», Макилрой приписал `grep` «безвозвратное внедрение» философии инструментов Томпсона в Unix.

### Использование

```
grep [OPTION]... PATTERNS [FILE]...
```

### Опции grep

| №   | Опции              | Описание                                                                 |
| --- | ------------------ | ------------------------------------------------------------------------ |
| 1   | `-e`               | Шаблон.                                                                  |
| 2   | `-i`               | Игнорирует различия регистра.                                            |
| 3   | `-v`               | Инвертирует смысл поиска соответствий.                                   |
| 4   | `-c`               | Выводит только количество совпадающих строк.                            |
| 5   | `-l`               | Выводит только совпадающие файлы.                                       |
| 6   | `-n`               | Предваряет каждую строку вывода номером строки из файла ввода.           |
| 7   | `-h`               | Выводит совпадающие строки, не предваряя их именами файлов.              |
| 8   | `-s`               | Подавляет сообщения об ошибках о несуществующих или нечитаемых файлах.   |
| 9   | `-f file`          | Получает регулярные выражения из файла.                                  |
| 10  | `-o`               | Печатает только совпадающие (непустые) части совпавшей строки.           |

Поддерживаются парные комбинации флагов (например, `-iv`, `-in`). Для регулярных выражений используется библиотека `regex` (POSIX).

## Тестирование

Интеграционные тесты расположены в директории `tests/`:

- `tests/test_cat.sh` — сравнение поведения `s21_cat` с системной утилитой `cat` для всех флагов (включая GNU-версии);
- `tests/test_grep.sh` — сравнение поведения `s21_grep` с системной утилитой `grep` для всех флагов и их парных комбинаций.

Каждый тест выполняет сравнение вывода реализованной утилиты с выводом системной утилиты через `diff`. Запуск — `make tests`.

## Структура проекта

```
.
├── Makefile              # единый Makefile для сборки и тестирования
├── README.md
├── src
│   ├── cat               # исходный код s21_cat
│   │   ├── cat.c
│   │   ├── cat.h
│   │   ├── test_cat.c    # точка входа (main)
│   │   ├── test.txt
│   │   └── test_2.txt
│   └── grep              # исходный код s21_grep
│       ├── grep.c
│       ├── grep.h
│       ├── test_grep.c   # точка входа (main)
│       └── test_files    # тестовые данные для grep
│           ├── test.txt
│           ├── test_2.txt
│           └── pattern.txt
└── tests                 # интеграционные тесты
    ├── test_cat.sh
    └── test_grep.sh
```

## Требования к реализации

- Язык C, стандарт C11, компилятор `gcc`.
- Ориентация на стандарт POSIX.1-2017, без устаревших и выведенных из употребления конструкций и библиотечных функций.
- Код написан в соответствии с Google Style.
- Ввод через `stdin` обрабатывать не обязательно.
- Реализация соответствует поведению реальных утилит Bash (учитываются отличия GNU-реализаций от решений BusyBox).